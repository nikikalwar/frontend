//getting tired so creating one script to create test cases for repititive string

/*
var inputs=prompt("enter the size of the test cases--no of test cases");
var upperbound =prompt("enter the max size of string");

*/
console.log(window.document.length);
for(prop in window.document){
   // console.log(prop);
    str=prop.toLowerCase();
    document.innerHTML=s;
    console.log(str);
}


//console.log(str);